# Data sources.
#
# This module declares none. Lookups belong to the root configuration, which
# resolves identifiers such as `subnet_id` and passes them in through the
# `virtual_machines` input. Keeping lookups out of the module makes it portable
# and easy to test.
